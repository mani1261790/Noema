{
  "name": "@noema/public-gate",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "check": "wrangler types src/worker-configuration.d.ts --config wrangler.jsonc && tsc --noEmit",
    "deploy:dry-run": "wrangler deploy --dry-run --config wrangler.jsonc",
    "deploy": "wrangler deploy --config wrangler.jsonc",
    "test": "node --test scripts/*.test.mjs",
    "types": "wrangler types src/worker-configuration.d.ts --config wrangler.jsonc"
  },
  "devDependencies": {
    "typescript": "^5.9.3"
  }
}
