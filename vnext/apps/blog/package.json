{
  "name": "@noema/blog",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "types": "wrangler types src/worker-configuration.d.ts --config wrangler.jsonc --strict-vars=false",
    "check:worker": "tsc --project tsconfig.worker.json",
    "generate:og": "node scripts/generate-og-images.mjs",
    "cms:migrate:local": "CI=true wrangler d1 migrations apply noema-cms --local --config ../studio/wrangler.jsonc --persist-to .wrangler/state",
    "validate:articles": "node scripts/validate-article-paths.mjs",
    "validate:content": "tsx scripts/validate-content.ts && npm run validate:articles",
    "test": "vitest run && node --test scripts/validate-article-paths.node-test.mjs",
    "predev": "npm run cms:migrate:local && npm run validate:content && npm run generate:og",
    "dev": "astro dev",
    "check": "npm run types && npm run validate:content && astro check && npm run check:worker",
    "prebuild": "npm run validate:content && npm run generate:og",
    "build": "astro build",
    "prebuild:preview": "npm run validate:content && npm run generate:og",
    "build:preview": "NOEMA_PREVIEW_CONTENT=1 astro build",
    "preview": "astro preview",
    "deploy:dry-run": "npm run build && wrangler deploy --dry-run --config dist/server/wrangler.json",
    "deploy:dry-run:production": "npm run build && wrangler deploy --dry-run --config dist/server/wrangler.json --name noema-learn-production --domain noema-learn.uk",
    "deploy": "npm run build && wrangler deploy --config dist/server/wrangler.json"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.2",
    "@astrojs/markdown-satteri": "0.3.3",
    "@noema/cms": "0.1.0",
    "@noema/content": "0.1.0",
    "@noema/ui": "0.1.0",
    "astro": "^7.0.7",
    "github-slugger": "^2.0.0",
    "highlight.js": "^11.11.1",
    "markdown-it": "^14.3.0",
    "satteri": "^0.9.5",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@astrojs/rss": "^4.0.19",
    "@types/markdown-it": "^14.1.2",
    "@types/node": "^24.13.3",
    "satori": "^0.26.0",
    "sharp": "^0.35.3",
    "tsx": "^4.21.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wawoff2": "^2.0.1",
    "yaml": "^2.9.0"
  }
}
